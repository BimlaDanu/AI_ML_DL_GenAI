### Diabetes Challenge

Here, we learned some interesting algorithms to solve classification problems. We implement new advanced data pre procssing algorithms compare their performance. We can find the challenge notebook [Diabetes Challenge](2_Diabetes_Challenge.ipynb). The first notebook will introduce the concept of **pipelines**. Sklearn provides us is with a module that makes the usage of pipelines fun and easy. 



#### Environment

```BASH
pyenv local 3.11.3
python -m venv .venv
source .venv/bin/activate
pip install --upgrade pip
pip install -r requirements.txt
```
